import ListCallers from "@/components/ListCallers";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Index() {
  return (
    <SafeAreaView>
      <ListCallers  />
    </SafeAreaView>
  );
}
